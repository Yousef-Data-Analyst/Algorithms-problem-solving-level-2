#include <iostream>
#include <cmath>
#include <string>
#include <iomanip>
using namespace std;


int readNonNegative(const string &message)
{
    int number;
    do
    {
        cout << message;
        cin >> number;

    } while (number < 0);
    return number;
}

void printInvertedNumberPattern(int num)
{
    for (int i = num; i > 0; i--)
    {
        for (int j = 1; j <= i; j++)
            cout << i << " ";
        cout << endl;
    }
}

int main()
{
    system("cls");
    printInvertedNumberPattern(readNonNegative("Enter a number to print an inverted pattern :) ? "));
}